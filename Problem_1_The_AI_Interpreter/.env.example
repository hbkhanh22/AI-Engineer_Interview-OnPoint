# Chế độ giả lập (True = chạy test không cần API Key, False = dùng API thật)
MOCK_MODE=True

# API Key Gemini (Chỉ cần điền khi MOCK_MODE=False)
# Đăng ký lấy key miễn phí tại: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

